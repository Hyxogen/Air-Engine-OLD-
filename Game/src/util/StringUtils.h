#pragma once

#include <iostream>
#include <sstream>
#include <fstream>
#include <vector>

namespace engine { namespace utility {

	std::vector<std::string> split(std::string* string, char delimeter);

}}